# Focused native36 Mac acceptance preparation

Not dispatchable yet. Public native35 remains unchanged. Native36 source is
d9924fa0748d10607196c4c6b4e686bb639cb7da; signed/stapled wheel and app receipts live
in the existing native36-candidate-20260912 audit directory. The final public
installer and release.json pins must be taken from the reviewed publication
staging before the production profile is changed from35 to36.

Use the existing single public standard ARM runner, 15-minute job bound and
12-minute installer/owner window. Pair only a newly created, GPU0 disposable
workspace with one Limited grant, provided in the dedicated temporary Actions
secret MESHIA_MAC36_PAIR_GRANT. No account OAuth, primary configuration or
existing workspace changes. Root must approve exact source/head and workspace.

Source-only owner preparation is in /tmp/meshia-mac36-owner. It contains the
existing envelope encryption, independent deadline watcher, exact host fences,
single normal workspace stop and cleanup polling. It deliberately has no
binding, grant, mutation receipt or valid public-kit head. The previous Mac35
episode's immutable bindings and encrypted responses were not copied.

After a clean public HTTPS install and native signature verification, the job
emits the exact hosted host/run/UID checkpoint. Root's ordinary owner MCP submits
mounted_replacement_probe.py once through the installed managed Python:

1. Prove Limited denies the existing synthetic personal canary.
2. Create/fsync/close two synthetic cache files, immediately replace the closed
   destination, and verify exact resulting bytes.
3. Hold an ordinary read descriptor open across replacement; verify it retains
   the old complete bytes and a newly opened pathname sees replacement bytes.

No broad Full/app matrix. This ordinary-descriptor test is intentionally stronger
than a backend fixture: SMB pathname-current semantics may expose a remaining
transport limitation and must not be silently treated as a pass.

The observer reads only the exact workspace database. Fresh singleton state is
accepted only when its private authenticated registry binds that exact UUID;
ambiguous state is rejected. No mount warming or unrelated workspace scan.

After checks, root disconnects only the test host through Account Settings.
Join exact generation/revoked_at with hosted local service/mount closure and
normal uninstall. Trigger the already armed watcher early, observe the original
stop's completion, capture completion, billing finality, deleted storage and all
zero cleanup counters. Remove the temporary secret after consumption; never
claim stale-signed-key or held-process proof without a dedicated observation.
